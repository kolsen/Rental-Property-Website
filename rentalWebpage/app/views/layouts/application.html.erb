<!DOCTYPE html>
<html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Rental Property Listing</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap-theme.min.css">
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
  </head>
   
  <div class="row">
    <div class="col-sm-12">
      <div class="container">
        <header>
          <h1>Rental Property Listing</h1>
        </header>
        
        <div id="user-widget">
          <% if current_user %>
            Welcome <strong><%= current_user.name %></strong>!
            <%= link_to "Sign out", signout_path, id: "sign_out" %>
          <% else %>
            <%= link_to "Sign in as Student", "/auth/facebook", id: "sign_in" %>
            or
            <%= link_to "Sign in as Landlord", logins_index_path %>
          <% end %>
        </div>
        <br>
        <%= form_tag property_listing_property_listing_path, :method => 'get' do %>
          <p>
            <%= text_field_tag :search, params[:search] %>
            <%= submit_tag "Search", :name => nil %>
          </p>
        <% end %>
        
        <p><strong><%= flash[:notice] %></strong></p>
        
        <section class="content">
          <%= yield %>
        </section>
        <footer>
          <h5>&copy;<%=Date.today.year %> Rental Property Listing</h5>
        </footer>
      </div>
    </div>
  </div>
</html>
