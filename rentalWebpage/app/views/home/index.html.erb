<br>
<table>
  <tr style="text-align: left">
    <th>Property Address (Address, City, State, Zip)</th>
    <th>Rating: </th>
    <th>Reviews: </th>
  </tr>
  <% if @properties != nil %>
    <% for property in @properties %>
      <tr>
        <td>
          <%= link_to (property.address + " " + property.apartment + ", " + 
          property.city + ", " + property.state + " " + property.zipcode), property %>
        </td>
        <td>(1-5)</td>
        <td><%= link_to property.reviews.count, property %></td>
      </tr>
    <% end %>
  <% end %>
</table><br>
<strong><%= link_to 'Add New Property', new_property_path %></strong>
